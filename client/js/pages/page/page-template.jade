div.page-header
div.page-container
  tweet(ng-repeat='tweet in vm.tweets', data='tweet')
  facebook-post(ng-repeat='post in vm.facebookPosts', data='post')
  google-post(ng-repeat='post in vm.googlePosts', data='post')
  instagram(ng-repeat='post in vm.instagrams', data='post')
div.page-footer